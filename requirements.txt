# Llaminar Python Dependencies
# PyTorch reference implementation for parity testing

# Core ML framework
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.6.0
torchvision  # Will auto-match torch version
torchaudio   # Will auto-match torch version

# HuggingFace ecosystem
transformers>=4.45.0
safetensors>=0.4.0
accelerate>=0.34.0

# Quantization support
bitsandbytes>=0.43.0

# Numerical computing (already in Dockerfile but specifying version)
numpy>=1.24.0

# Testing
pytest>=8.0.0

# Data handling
sentencepiece>=0.2.0
tokenizers>=0.19.0

# Optional: for model conversion/loading
huggingface-hub>=0.24.0
